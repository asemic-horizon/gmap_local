import setuptools
from setuptools import setup
import os



core_dependencies = []

print(core_dependencies)
setup(name="gmaps_local",
      version='0.01',
      description='APIs',
      author='Diego Navarro',
      author_email='the.electric.me@gmail.com',
      license='MIT',
      #packages=setuptools.find_packages(),
      # install_requires=deps,
      # dependency_links=core_dependencies,
      zip_safe=False)

